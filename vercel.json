{
  "version": 2,
  "framework": "vite",
  "buildCommand": "cd code-quest && npm ci && npm run build",
  "outputDirectory": "code-quest/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
